#include <bits/stdc++.h>
using namespace std;
void solve(){
    int n;
    cin>>n;

    while(n--){
        int a;
        cin >> a;

        string s;
        cin >> s;

        
        cout << s.back() << endl;
}
}
int main(){ 
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
        solve();
}